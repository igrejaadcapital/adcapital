# Copie para .env (não commitar credenciais reais)

# API Django — preferir /api/v1/ (legado /api/ ainda funciona no backend)
VITE_API_URL=http://127.0.0.1:8000/api/v1

# Build Android: npm run build:mobile já fixa a API de produção (ignora este .env).
# API local no emulador NÃO use 127.0.0.1 — use http://10.0.2.2:8000/api/v1

# Google Analytics 4
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
